import {fetchPokemonInformation, fetchPokemonItemList, fetchPokemonMoreInformation} from "./PokemonRepository.ts";

export const PokemonEntity = {
    fetchPokemonInformation,
    fetchPokemonItemList,
    fetchPokemonMoreInformation
}